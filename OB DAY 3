import java.io.*;
import java.util.*;
import java.text.*;
import java.math.*;
import java.util.regex.*;

public class Solution {

    public static void main(String[] args) {
        /* Enter your code here. Read input from STDIN. Print output to STDOUT. Your class should be named Solution. */
        Scanner  scanner = new Scanner(System.in);
        int a = scanner.nextInt();
        if(a%3==0 && a%5==0)
            System.out.println("HiHello");
        else if(a%3==0)
            System.out.println("Hi");
        else if(a%5==0)
            System.out.println("Hello");
    }
}
import java.io.*;
import java.util.*;
import java.text.*;
import java.math.*;
import java.util.regex.*;

public class Solution {

    public static void main(String[] args) {
        /* Enter your code here. Read input from STDIN. Print output to STDOUT. Your class should be named Solution. */
        Scanner sc =new Scanner(System.in);
        String da=sc.nextLine();
        String []parts=da.split("/");
        int date=Integer.parseInt(parts[0]);
        int month=Integer.parseInt(parts[1]);
        int year=Integer.parseInt(parts[2]);
        if (date >= 1 && date < 31 && month >= 1 && month <= 12 &&month!=2 && year >= 1900 && year <= 9999) {
            System.out.println("Valid");
        } else {
            System.out.println("Invalid");
        }
        if (date >= 1 && date < 29 && month == 2 && year >= 1900 && year <= 9999){
            
            System.out.println("Valid");
        }
        else if(date >= 1 && date > 29 && month == 2 && year >= 1900 && year <= 9999){
            System.out.println("Invalid");
        }
    }

import java.util.Scanner;
public class index {
    public static void main(String[] args) {
        Scanner sc=new Scanner(System.in);
        int a=sc.nextInt();
        int b=sc.nextInt();
        char n=sc.next().charAt(0);
        switch(n){
            case '+':
                System.out.println(a+b);
                break;
            case '-':
                System.out.println(a-b);
                break;
            case '*':
                System.out.println(a*b);
                break;
            case '/':
                if(b==0)
                    System.out.println("zero is not allowed in denominator");
                else
                    System.out.println(a/b);
                break;
            case '^':
                System.out.println((int)Math.pow(a, b));
                break;
            default:
                System.out.println("Invalid");
        }
        
    }
}
